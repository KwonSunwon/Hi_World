** 이중포인터

int n;

	while ( true ) {
		cout << "동물을 몇 마리를 받을까요? ";
		cin >> n;

		Animal** room = new Animal*[n];

		for ( int i = 0; i < n; ++i ) {
			if ( uid ( dre ) )
				room[i] = new Dog;
			else
				room[i] = new Cat;
		}

		for ( int i = 0; i < n; ++i ) {
			cout << i + 1 << "번째 방 : ";
			room[i]->speak ( );
		}
		
		for ( int i{} ; i < n ; ++i )
			delete[] room[i];
		delete[] room;
	}
